-- ============================================================
-- 0041_doc_rules_by_path.sql · الأوراق تتبع المسار
-- ============================================================
-- القاعدة: لا نعرض ورقة لا تخصّه، ولا نخفي ورقة تخصّه.
--   إثبات الملكية لمن يرمّم: لمن يملك الدار (ملكه أو ورثة) — الكاري
--   يُوقَف قبل الوصول إلى الأوراق أصلاً.
--   رخصة الترميم/التوسعة: حين يُبنى شيء — توسعة أو طابق — لا لدهان ولا لسباكة.
-- الشروط تُقرأ في lib/request-documents.ts (home_owner · structural).
-- ============================================================

update request_doc_catalog set only_if = '{home_owner}'
  where code in ('land_title', 'ownership_cert');

update request_doc_catalog set only_if = '{structural}'
  where code = 'renovation_permit';
